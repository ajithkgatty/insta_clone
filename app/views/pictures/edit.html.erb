<%= render 'form' %>
<div class="text-center edit-links">
  <%= link_to "Delete Picture", picture_path(@picture), method: :delete, data: { confirm: "Are you sure you want to delete this post?" } %>
  ||
  <%= link_to "cancel", pictures_path %>
</div>
